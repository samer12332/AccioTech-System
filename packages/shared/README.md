# Shared package

This directory is reserved for small, genuinely reusable TypeScript contracts shared by AccioTech applications. No shared package or domain types are defined until a concrete use case requires them.
